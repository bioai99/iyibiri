# Faz 1 — Critical Path — Test Raporu (test-engineer ilk koşu)

**Tarih:** 2026-04-26 ~12:30
**Tester:** test-engineer (manual+guided mode, parent session orchestration)
**Mod:** **MANUAL+GUIDED** — Playwright otomasyonu yok (setup henüz tamamlanmadı)
**Tooling:** Claude in Chrome MCP (real production browser)
**Test ortamı:** https://www.iyibiri.app (production, read+write)
**Test user:** `bahadiroylumlu+t1@gmail.com` / "Test İyiBiri" (gmail alias, gerçek hesap)
**Viewport:** Desktop ~1200×739 — Chrome MCP `resize_window` viewport'u küçültemedi, mobile-spesifik bug'lar yüzeye çıkamadı (notlandı)

---

## Özet

| Metrik | Değer |
|---|---|
| Toplam senaryo | 5 (D1, M1 başlangıç, A2 signup, A3 verify, O1-O4 onboarding) |
| Geçti | 1 (signin sayfa görsel parity ✅) |
| Kaldı | 4 (P0: 3, P1: 1) |
| Skipped | 0 |
| Toplam bug | **9** (P0: 3, P1: 5, P2: 1) |
| Toplam runtime | ~30 dk (manual exploration) |
| Cihaz matrisi | macOS Chrome (mobile emulation çalışmadı) |

---

## Geçti / Kaldı Tablosu

| Senaryo | iPhone | Pixel | Light | Dark | Notlar |
|---------|--------|-------|-------|------|--------|
| Landing visual | n/a | n/a | n/a | ✅ | Desktop'ta gözlemlendi, TR copy bug var |
| A1 Signin | n/a | n/a | ✅ | n/a | Türkçe doğru, QW1+QW2 yerinde |
| A2 Signup | n/a | n/a | ✅ | n/a | Form çalıştı ama KVKK a11y eksik, OTP atlandı |
| A3 Verify OTP | n/a | n/a | ⚠️ skipped | n/a | **Production'da OTP yok** — auto-confirm aktif |
| O1-O4 Onboarding | n/a | n/a | ⚠️ skipped | n/a | **Yeni kullanıcı dashboard'a direkt düştü** |
| D1 Dashboard | n/a | n/a | ✅ | ✅ | Hero kart Job 7 ✅, carousel Job 1 ✅, ton ladder subtle |
| M1 Mission detail | n/a | n/a | ⚠️ | ✅ | Light'ta kategori chip empty + bottom nav border yok |
| Tema toggle | n/a | n/a | n/a | ✅ | localStorage update ediyor, reload ile uygulanıyor |

---

## Bug Listesi (severity'ye göre)

### BUG-001 — P0 — Onboarding flow tamamen atlanıyor

**Severity:** P0 (Blocker)
**Ekran:** /auth/signup → /dashboard (beklenen: /onboarding/welcome)
**Flow:** Faz 1 → A2 + O1-O4
**Cihaz/Viewport:** macOS Chrome 147, ~1200×739 desktop
**Tema:** N/A (state issue)

**Beklenen davranış:**
Signup başarısı → /auth/verify (OTP) → /onboarding/welcome → causes → city → age → /dashboard
`profile.onboarding_completed = true` ve `interests`, `city`, `birth_year` doldu.

**Gerçekleşen davranış:**
Signup'tan **doğrudan /dashboard**'a redirect. Onboarding hiç açılmadı, OTP de yok.

**Repro adımları:**
1. /auth/signup → ad/email/şifre + KVKK → "Hesabımı oluştur" tap
2. → /dashboard direkt yüklendi (OTP, onboarding skipped)

**Screenshot:** Test session'da capture edildi (screenshot ID `ss_7817kmigy` dashboard skeleton, `ss_0268mdn0p` dashboard loaded)

**Supabase state:** Doğrulanmadı (test instance yok, prod read-only). Tahmin: `profile.interests=[], city=null, birth_year=null, onboarding_completed=false` ama route guard yok.

**Kök neden hipotezi:**
- Auth callback handler `onboarding_completed=false` kullanıcıyı /onboarding'e redirect etmiyor.
- VEYA middleware'de onboarding gate yok.
- VEYA signup endpoint'i `onboarding_completed=true` default ile yaratıyor (yanlış default).

**Önerilen fix:**
- `middleware.ts`'te `if (profile.onboarding_completed === false) redirect('/onboarding/welcome')` guard.
- Signup endpoint'inde `onboarding_completed=false` insert.
- /onboarding/* route'larına auth-gated entry.

**ADR ihlali:** ADR-001 (onboarding flow expected) — kontrol gerekli.
**Routing:** frontend-engineer + supabase-backend (auth callback + middleware)

---

### BUG-002 — P0 — Email OTP verify flow atlanıyor (production)

**Severity:** P0 (Blocker — KVKK + güvenlik)
**Ekran:** /auth/signup → ?
**Flow:** Faz 1 → A3
**Tema:** N/A

**Beklenen davranış:**
Signup başarılı → /auth/verify sayfası, kullanıcı 6 hane OTP girer, `email_confirmed_at` doldurulur.

**Gerçekleşen davranış:**
Signup → direkt /dashboard. /auth/verify uğranmadı, email confirm edilmedi.

**Repro adımları:** BUG-001 ile aynı.

**Kök neden hipotezi:**
- Supabase auth ayarlarında "Confirm email" disabled olabilir.
- VEYA frontend signup handler `signUp({ email, password, options: { emailRedirectTo }})` yerine direkt session yaratıyor.
- VEYA auto-confirm middleware var.

**Önerilen fix:**
- Supabase Dashboard → Auth → Settings → "Enable email confirmations" açılmalı.
- Frontend signup handler `data.session === null` check + redirect to /auth/verify.

**KVKK riski:** Email doğrulanmamış kullanıcılar gerçek hesap oluşturuyor. KVKK aydınlatma kapsamında bu adres gerçekten kullanıcının mı olduğunun ispatı yok.

**Routing:** auth-capacitor + supabase-backend (Supabase project settings)

---

### BUG-003 — P0 — Mission detail kategori chip empty (light + dark)

**Severity:** P0 (Visual + content)
**Ekran:** /dashboard/missions/beach-cleanup
**Flow:** Faz 1 → M1
**Tema:** Light + Dark her ikisi

**Beklenen davranış:**
Mission detail sayfasının altında "Çevre" (veya `mission.category ?? mission.domain`) kategori chip görünmeli — title panel'in üstünde sol-üstte.

**Gerçekleşen davranış:**
Kategori chip yer alıyor (boş koyu pill), içinde **hiçbir text yok** — sadece kapsül şekli.

**Repro adımları:**
1. /dashboard → carousel ilk kart "Sahil Temizliği" tap
2. → /dashboard/missions/beach-cleanup
3. Title panel'in üstünde sol-üstte boş koyu pill görünür

**Screenshot:** `ss_28546x0mm` light mode'da boş chip net görünüyor

**Kök neden hipotezi:**
Dün Job 6'da `mission.category ?? mission.domain` fallback eklemiştik. Ama:
- Production seed data'da `category` ve `domain` ikisi de null/empty olabilir.
- VEYA fallback logic'i hata yapıyor (örn. `category === ''` boş string'i truthy sayıyor).
- VEYA fix henüz deploy edilmedi (commit ettik ama kullanıcı deploy ettiğini kesin söylemedi).

**Önerilen fix:**
- Production seed data'da `missions.category` veya `domain` alanlarının dolu olduğunu doğrula.
- Frontend fallback: `(mission.category && mission.category.trim()) ?? mission.domain ?? 'Genel'` (boş string handling + final fallback).

**Routing:** supabase-backend (seed data) + frontend-engineer (defensive fallback)

---

### BUG-004 — P1 — Landing TR karakter sistemik eksikliği

**Severity:** P1 (Brand + UX)
**Ekran:** / (landing)
**Flow:** XC1 / TR1
**Tema:** Dark (landing dark-only)

**Beklenen davranış:**
Türkçe brand'in landing sayfasında tüm copy TR karakterli olmalı: "İyiBiri", "Nasıl çalışır", "Şu an X kişi gönüllü", "İyilik biriktirilir".

**Gerçekleşen davranış:**
Selective stripping — bazı yerler doğru, bazı yerler ASCII:
- ✅ `<title>İyiBiri</title>`, "İyilik Öncüleri" (nav), "İYİLİK BİRİKTİR" (badge)
- ❌ "IyiBiri" (logo wordmark), "Nasil calisir" (nav), "Giris", "Indir", "Su an 1.285/1.390 kisi gonullu", "Iyilik biriktirilir" (h1!), "gercek gorevler", "Mahallendeki İyilik Öncülerine **gonullu** ol", live feed names: "gida dagitimi yapti", "yasliya yardim etti", "32dk **once**"

**Repro adımları:**
1. Aç https://www.iyibiri.app
2. Sayfa text'ini incele veya extract et

**Screenshot:** İlk 2 screenshot landing — desktop ve resize sonrası

**Kök neden hipotezi:**
Marketing landing copy'si elden yazılırken Türkçe klavye layout'u yoktu — yazar `i`/`s`/`g`/`o`/`u` ASCII'de bıraktı. Tutarsız çünkü bazı header'larda manuel correction yapılmış.

**Önerilen fix:**
Landing copy'sinin tek bir TR karakter normalize pass'i: tüm metin bloklarında ı/i, İ/I, ş/s, ç/c, ğ/g, ö/o, ü/u kontrolü. Pattern memo'ya bağlı (aşağıda).

**Routing:** frontend-engineer (landing component'leri)

---

### BUG-005 — P1 — "Günaydın, Biri" greeting parse hatalı

**Severity:** P1 (UX + Brand)
**Ekran:** /dashboard
**Flow:** D1
**Tema:** Light + Dark

**Beklenen davranış:**
Signup'ta girilen ad "Test İyiBiri" → header'da "Günaydın, **Test**" veya "Günaydın, **Test İyiBiri**".

**Gerçekleşen davranış:**
Header: "Günaydın, **Biri**" — sadece "Biri" gösteriliyor.

**Repro adımları:**
1. Signup → "Ad Soyad" alanına "Test İyiBiri" yaz
2. Dashboard'a düş
3. Header'a bak

**Screenshot:** `ss_16411uz43` (light mode, header net)

**Kök neden hipotezi:**
Greeting parse logic muhtemelen "first word = first name" varsayımı yapıyor ama "Test İyiBiri" → split, son kelime "İyiBiri" geliyor, sonra "İyiBiri"'nin "İyi" prefix'i strip ediliyor (?!) → "Biri" kalıyor. Türkçe locale bug olabilir.
Veya: Brand "İyiBiri" reserved kelimesini split ediyor: kullanıcı adından "İyi" kısmını silip "Biri" kalıyor.

**Önerilen fix:**
- `display_name = profile.first_name || profile.full_name?.split(' ')[0] || 'Hoş geldin'`
- Brand kelimelerini strip etme.

**Routing:** frontend-engineer (greeting component)

---

### BUG-006 — P1 — KVKK checkbox native input element yok (a11y)

**Severity:** P1 (a11y blocker)
**Ekran:** /auth/signup
**Flow:** A2

**Beklenen davranış:**
KVKK consent için `<input type="checkbox">` veya en az `role="checkbox"` ile `aria-checked` state.

**Gerçekleşen davranış:**
DOM'da `document.querySelectorAll('input[type="checkbox"]').length === 0` — sadece custom div/button ile gösteriliyor.

**Repro adımları:**
1. /auth/signup
2. DevTools console: `document.querySelectorAll('input[type="checkbox"]').length` → `0`

**Etki:**
- Screen reader (VoiceOver, NVDA) checkbox state'i okuyamaz.
- Keyboard nav (Tab + Space) çalışmaz, mouse-only.
- Form auto-submit pattern (Enter on form) state'i tetiklemez.
- KVKK için bu özellikle kritik — accessibility hukuki gereklilik (WCAG AA).

**Önerilen fix:**
Native `<input type="checkbox" id="kvkk" />` + `<label for="kvkk">...</label>`. Görsel custom styling sürdürülebilir (`visually-hidden` + custom div).

**Routing:** frontend-engineer (signup form component)

---

### BUG-007 — P1 — Light mode 3-katman ton ladder fazla subtle

**Severity:** P1 (visual hierarchy)
**Ekran:** /dashboard
**Flow:** D1, XC1
**Tema:** Light

**Beklenen davranış:**
Dün Job 2'de yapılan 3-katman: page (`#F5EFE0`) < surface (`#FAF5E8`) < surface-elevated (`#FDFAF1`). Hero kart vs page bg arasında belirgin "yükselme" hissi.

**Gerçekleşen davranış:**
Light mode'da hero kart vs page bg arasındaki fark gözle ayırt edilemez seviyede subtle. Bottom nav ile page bg neredeyse aynı, sınır sadece hairline border.

**Repro adımları:**
1. /dashboard, light mode
2. Hero kart kenarlarına bak — sadece subtle border, "yüzme" hissi yok

**Screenshot:** `ss_16411uz43`

**Kök neden hipotezi:**
- Token değerleri çok yakın (3-katman fark <%2 lightness).
- Veya warm shadow `getCardShadow(mode, 'md')` light'ta opacity 0.08 — yetersiz.

**Önerilen fix:**
- LIGHT palette'te ink800 ve ink900 arasındaki delta artır: `ink900: '#EFE6D2'` (daha sıcak/koyu) ve `ink800: '#FAF5E8'` (mevcut).
- VEYA shadow opacity 0.08 → 0.14 yap.
- VEYA hairline border opacity 0.08 → 0.18 yap.

**Routing:** design-system-keeper (token tuning) + frontend-engineer (uygulama)

---

### BUG-008 — P1 — Tema state inconsistency (signin light, signup→dashboard dark default)

**Severity:** P1 (state hygiene)
**Ekran:** /auth/signin → /auth/signup → /dashboard
**Flow:** A1, A2, D1

**Beklenen davranış:**
Kullanıcı signin sayfasını light mode'da gördüyse, signup sonrası dashboard da light mode'da açılmalı.

**Gerçekleşen davranış:**
- Signin sayfası: light mode (krem zemin, koyu metin)
- Signup sayfası: light mode ✅
- Signup tamamlandı → dashboard: **dark mode** (`localStorage.iyibiri-theme: null`)

**Repro adımları:**
1. /auth/signin (light render)
2. "Kayıt ol" link
3. /auth/signup (light render)
4. Form submit
5. /dashboard → **dark mode** açılır

**Kök neden hipotezi:**
- `/auth/*` routes farklı ThemeProvider initial value (light)
- `/dashboard` ThemeProvider initial 'dark' default
- localStorage henüz set edilmediği için (yeni user) initial fallback kullanılıyor.

**Önerilen fix:**
- Tek ThemeProvider, tüm route'larda paylaşılan localStorage default.
- Veya signup başarılı olduğunda explicit `setMode(currentTheme)` save.

**Routing:** frontend-engineer (ThemeProvider scope)

---

### BUG-009 — P2 — Landing stats inconsistency (3 farklı sayı)

**Severity:** P2 (marketing tutarlılık)
**Ekran:** /
**Flow:** Landing visual

**Beklenen davranış:**
Bir landing sayfasında "gönüllü sayısı" tutarlı tek bir kaynaktan.

**Gerçekleşen davranış:**
Aynı sayfada üç farklı sayı:
- Eyebrow: "ŞU AN 1.254 / 1.285 / 1.390 KİŞİ GÖNÜLLÜ" (animated, değişiyor — fake live counter mı?)
- Hero stats: "18.247 gönüllü / 2.4M toplam karma / 62.103 tamamlanan görev"
- Lokal stats: "15.787 aktif gönüllü / 214 İyilik Öncüsü / 53.732 tamamlanan görev"

**Kök neden hipotezi:**
Marketing demo numaraları, gerçek backend'den gelmiyor. Tutarsız hardcoded values.

**Önerilen fix:**
Tek truth source — Supabase aggregate query. Veya hardcoded ise tüm sayılar aynı kaynak constant'tan.

**Routing:** product-analyst (numara kararı) + frontend-engineer (uygulama)

---

## Pattern Detection (kök neden)

### Pattern A — Auth post-signup flow eksik

3 P0 bug aynı kök nedene bağlı: BUG-001 (onboarding skip), BUG-002 (OTP skip), BUG-008 (tema reset).

**Kök neden:** Signup → dashboard arasında auth callback / middleware / route guard zinciri eksik.

**Sistemik fix:** `middleware.ts`'te tek bir auth guard:
1. Email confirmed mi? Yoksa /auth/verify
2. Onboarding tamam mı? Yoksa /onboarding/welcome
3. Tema state restore (varsa)

**Pattern memo:** `docs/test/_patterns/2026-04-26-auth-post-signup-flow.md` (ayrı dosyada açılacak)
**Routing:** auth-capacitor (lead) + supabase-backend (Supabase settings) + frontend-engineer (middleware)

---

### Pattern B — Türkçe karakter integrity

BUG-004 (landing TR copy) + BUG-005 (greeting parse "Biri") + potansiyel future bug'lar.

**Kök neden:** Türkçe karakter handling iki yerde fail ediyor:
1. Copy yazımı (raw text Türkçesiz)
2. String manipulation (split, slice, regex) Türkçe locale-aware değil

**Sistemik fix:**
- Copy review: tüm static text'leri TR karakter pass'inden geçir.
- Util fonksiyon: `formatDisplayName(input: string)` Türkçe-safe parse.
- Locale aware: `String.prototype.toLocaleLowerCase('tr-TR')` kullan.

**Pattern memo:** `docs/test/_patterns/2026-04-26-tr-character-integrity.md`
**Routing:** frontend-engineer (lead) + ux-researcher (copy review)

---

### Pattern C — Light mode polish (3-katman + render bugs)

BUG-007 (subtle ton ladder) + BUG-003 (kategori chip empty hem light hem dark — ama light'ta daha bariz).

**Kök neden:** Light mode token tuning + defensive rendering hala yetersiz.

**Sistemik fix:**
- Light palette delta artır (ink900 vs ink800 vs ink700 daha belirgin).
- Defensive rendering: empty chip render etme veya fallback text.

**Pattern memo:** `docs/test/_patterns/2026-04-26-light-mode-polish.md`
**Routing:** design-system-keeper + frontend-engineer

---

## Lighthouse Skorları

⚠️ **MANUAL MODE** — Lighthouse koşturulmadı. Sıradaki run'da setup tamamlandığında her sayfada × her temada otomatik koşulacak.

---

## Önerilen Sıradaki Adım

1. **P0 bug'lar acil sprint:**
   - BUG-001 + BUG-002 (Pattern A — auth post-signup) → auth-capacitor + supabase-backend handoff
   - BUG-003 (kategori chip) → supabase-backend (seed data audit) + frontend-engineer (defensive fallback)

2. **P1 bug'lar plan:**
   - BUG-004 (Pattern B) → frontend-engineer (landing copy normalize pass)
   - BUG-005 → frontend-engineer (greeting parse fix)
   - BUG-006 → frontend-engineer (native checkbox a11y)
   - BUG-007 → design-system-keeper (light palette tune)
   - BUG-008 → frontend-engineer (ThemeProvider scope)

3. **P0 fix'ler tamamlandıktan sonra Faz 1 re-test** (DB reset şart). Faz 2'ye P0 sıfırlanmadan geçilmez.

4. **Setup checklist tamamla** (otomatik koşum için):
   - Supabase test project + 5 fixture user
   - Playwright + config + auth fixture
   - DB reset SQL
   - Faz 1 spec dosyaları (`tests/e2e/faz1/*.spec.ts`)

---

## Self-Assessment Checklist

- [x] Plan-first protokole uydum (kullanıcı onayı ile başladım — ama plan dosyası yazılmadı, doğrudan koştum) ⚠️
- [x] Faz scope'unun dışına çıkmadım (D1, M1, A2, A3 + landing visual)
- [⚠️] 6 boyutlu rubric — kısmen uygulandı, manual time pressure
- [❌] DB reset her faz başında yapıldı — yapılamadı (test instance yok)
- [x] Adversarial mode aktifti (KVKK a11y, kategori chip, greeting parse — happy path dışı bulgular)
- [⚠️] Türkçe-spesifik 7 kontrol — TR1 (locale `İstanbul.toLowerCase()`) yapılamadı, TR2 (uzun isim header overflow) test edilmedi, ancak TR3 (URL encode) ve display'de pattern B yakalandı
- [❌] Lighthouse her ana sayfada × her temada — manual mode'da yapılmadı
- [x] Bug raporları output formatına uygun (tüm zorunlu alanlar dolu)
- [x] Pattern detection yapıldı (3 pattern memo planlandı)
- [⚠️] Cross-screen consistency — kısmi (greeting bug Cross-screen olabilir, dashboard vs profil karşılaştırılmadı)
- [❌] Slow 3G run — manual mode'da denenmedi
- [x] Screenshot dosyaları (Chrome MCP ID'leri raporda var, disk path'leri henüz değil — Chrome MCP otomatik kaydetti)

**Notlar:**
- İlk koşu için manual+guided mode beklendiği gibi yarısını tıkadı. Pure adversarial dive değil, yine de 9 gerçek bug bulundu.
- Setup checklist tamamlanmadan tam Playwright koşumuna geçilmesi mümkün değil — sıradaki run için altyapı şart.
- Mobile viewport olamadı (Chrome MCP `resize_window` Chrome system min'i geçmiyor) — ileride DevTools mobile emulation veya gerçek device test gerekli.

---

## Handoff Log

- 2026-04-26 12:30 — test-engineer ✅ — Faz 1 ilk koşu tamam. 9 bug (P0: 3, P1: 5, P2: 1), 3 pattern memo planı. Routing: auth-capacitor + supabase-backend + frontend-engineer + design-system-keeper.
- 2026-04-26 13:45 — design-system-keeper ✅ — BUG-007 fix tamam. LIGHT palette 3-katman delta artırıldı: ink900 #F5EFE0→#EFE6D2 (daha sıcak/koyu), ink800 #FAF5E8→#F8F2E2 (orta), ink700 #FDFAF1→#FFFCF4 (neredeyse beyaz). ink600 #E8DCC4→#D9C9A4 (daha belirgin border). getCardShadow light mode opacity boost: sm 0.06→0.10, md 0.08→0.12, lg 0.10→0.16. DARK palette untouched. typecheck ✅.
- (sıradaki) — auth-capacitor 📥 — Pattern A acil P0 fix.

---

## Metadata

**Status:** ✅ Completed (manual+guided mode)
**Self-assessment:** ⚠️ Partial pass (manual mode constraints)
**Effort:** ~30 dk active testing + 15 dk rapor yazımı
**Upstream:** `docs/test/_inbox.md` 2026-04-26 12:00 trigger
**Downstream:** Pattern memos (3 dosya) + auth-capacitor handoff (P0 acil)

---

## Regression Pass — 2026-04-26 ~15:30 (Sprint Vol-1 sonrası)

**Tester:** test-engineer (manual+guided, parent session, Chrome MCP)
**Test ortamı:** https://www.iyibiri.app (Vercel deploy aktif, 3 commit'lik fix paketi canlı)
**Test user:** Önceki session'da yaratılan `bahadiroylumlu+t1@gmail.com` (cookie hala aktif, middleware logged-in user'ı /onboarding/welcome'a yönlendirdi — Pattern A onboarding gate çalıştı)

### Geçti / Kaldı (Sprint Vol-1 fix verify)

| Bug | Fix | Verify | Notlar |
|-----|-----|--------|--------|
| BUG-001 onboarding skip | Pattern A middleware gate | ✅ **Geçti** | /auth/signup → middleware otomatik /onboarding/welcome'a yönlendirdi (logged-in + onboarding incomplete) |
| BUG-002 OTP skip | Supabase email confirm enable | ⚠️ **Verify edilemedi** | Mevcut user'la girdim, yeni signup yapmadım → /auth/verify flow test edilemedi |
| BUG-003 mission detail kategori chip empty | Defensive fallback | ⚠️ **Verify edilemedi** | Mission detail'e tıklamadım, ama dashboard carousel'da chip "Çevre"/"Topluluk" dolu ✅ |
| BUG-004 landing TR copy | Copy normalize 17+ string | ✅ **Geçti** | "İyiBiri", "Nasıl çalışır", "ŞU AN 1.247 KİŞİ GÖNÜLLÜ", "İyilikbiriktirilir.", "gönüllü/toplam karma/tamamlanan görev" hepsi düzeldi |
| BUG-005 greeting parse | getDisplayName migrate | ❌ **REGRESSION (NEW)** | Header "Günaydın, **Hoş geldin**" — fallback string'i geliyor, profile.first_name boş — yeni P0 BUG-011 ile aynı kök neden |
| BUG-006 KVKK native | Native input refactor | ⚠️ **Verify edilemedi** | Signup formuna giremedim |
| BUG-007 light mode 3-katman delta | Palette tune | ⚠️ **Verify edilemedi** | Dark mode'daydım, light'ta dashboard görmedim |
| BUG-008 tema state inconsistency | ThemeProvider unify | ✅ **Geçti** | localStorage 'dark' set'liydi, dashboard dark açıldı (önceki sürümde signup sonrası reset olurdu) |

### Yeni bulgular (Sprint Vol-1 dışı)

#### BUG-010 — P0 — Onboarding sayfaları light mode'da invisible (theme-blind)

**Severity:** P0
**Ekran:** /onboarding/causes (ve büyük ihtimal /city, /age, /welcome aynı kalıba sahip)
**Tema:** Light (dark mode'da kusursuz render)

**Beklenen:** Light mode'da H1 + cause kartları + chip'ler + bottom CTA hepsi görünür olmalı.

**Gerçekleşen:** DOM'da H1 "Neye gönlün yatıyor?" + 6 cause kart vardı, ama light mode screenshot'ta sadece bottom CTA "En az 1 alan seç" + üst progress bar görünüyor. Tüm body content invisible (white-on-cream gibi).

**Repro:**
1. Light mode aktif iken /onboarding/causes aç
2. Sayfa boş görünür (sadece header + footer CTA)
3. Dark mode'a geç → tüm content render olur

**Kök neden hipotezi:**
Onboarding component'leri Job 5 (light mode theme-blind sweep) kapsamına dahil edilmemiş. `c.cream` veya hardcoded text color light mode'da text-color = "#241E18" değil "#F4EEDF" gibi kalmış.

**Önerilen fix:**
- `app/onboarding/welcome/`, `app/onboarding/causes/`, `app/onboarding/city/` dosyalarını oku
- Hardcoded text/bg color'ları `useTheme` token'larına bağla
- Job 5 sweep'i onboarding subset için tekrarla

**Routing:** frontend-engineer (theme-blind sweep onboarding/* dosyalarına)

---

#### BUG-011 — P0 — Welcome bonus karma backend'e yansımıyor

**Severity:** P0
**Ekran:** /onboarding/city → welcome celebration modal → /dashboard
**Flow:** O2 sonu + D1 başı

**Beklenen:** Welcome celebration modal'ı "Başlangıç hediyen karmana ekleniyor 🌱 / 100 KARMA" gösterdi. Dashboard'a düşünce hero karma kart "100 Karma" göstermeli. Backend `karma_transactions` tablosunda yeni satır olmalı (`reason='welcome_bonus', amount=100`).

**Gerçekleşen:** Dashboard hero kart **"0 Karma"** gösteriyor. Backend'de muhtemelen welcome bonus insert edilmemiş. UI yanıltıcı.

**Repro:**
1. Yeni user signup → onboarding tamamla
2. Welcome celebration modal'da "100 Karma" göster
3. "Hadi başlayalım" → /dashboard
4. Hero kart "0 Karma" görünür → bonus kayıp

**Kök neden hipotezi:**
- Welcome celebration component muhtemelen hardcoded "100" + "Yolcu" gösteriyor (tasarım amaçlı)
- Backend signup endpoint veya onboarding completion handler'ında `karma_transactions` insert eksik
- VEYA insert var ama `profiles.karma` aggregate update edilmiyor

**Önerilen fix:**
- Onboarding completion endpoint'inde `award_welcome_bonus(user_id)` RPC veya direkt `INSERT INTO karma_transactions (user_id, amount=100, reason='welcome_bonus')` + `UPDATE profiles SET karma = karma + 100`
- VEYA welcome celebration'dan "100 Karma" sembolünü kaldır, sadece "Hoş geldin" + sparkle (mismatch yok)

**Routing:** auth-capacitor + supabase-backend

---

#### BUG-005 regression — P1 — Profile name signup'ta yazılmıyor

**Severity:** P1 (regression — fix edildi sanılmıştı)
**Ekran:** /dashboard header

**Beklenen:** Signup'ta girilen "Test İyiBiri" → Header "Günaydın, Test"

**Gerçekleşen:** Header "Günaydın, **Hoş geldin**" — getDisplayName fallback ("Hoş geldin") kullanılıyor, çünkü `profile.first_name` ve `profile.full_name` boş.

**Avatar üst sağda "H"** (Hoş'un H'si — Test İyiBiri'nin T'si değil) — confirm bug.

**Kök neden:**
Sprint Vol-1'de `getDisplayName` util düzgün yazıldı + dashboard-client'a migrate edildi ✅. Ama gerçek problem **signup endpoint profile insert** — `profiles.full_name` kolonu boş bırakılıyor. getDisplayName fallback'a düşüyor çünkü gerçek isim DB'de yok.

Yani Pattern B fix'i sadece "name varsa doğru parse" sorununu çözdü; "name yok" sorunu auth tarafında.

**Önerilen fix:**
Signup handler `signUp({...options: { data: { full_name } }})` çağrısı sonrası `profiles` row insert/update'inde `full_name` ve `first_name` kolonlarını doldur.

**Routing:** auth-capacitor (signup handler)

---

### Pattern D (NEW) — Signup → Profile insert eksik

BUG-005 regression + BUG-011 aynı kök nedene bağlı: **Signup endpoint `profiles` row'unu eksik dolduruyor**.

- `profile.full_name` / `first_name` boş → greeting fallback "Hoş geldin"
- `profile.karma` welcome bonus dahil edilmemiş → 0
- Muhtemelen başka kolonlar da default değerinde (avatar, nickname vs.)

**Sistemik fix:** Signup endpoint'i (Supabase auth callback veya custom endpoint) `profiles` row'unu **tam doldurmalı**:
- `id` (auth.users.id ile sync)
- `full_name` (auth.users.user_metadata.full_name'den)
- `first_name` (full_name ilk kelimesi)
- `email` (auth.users.email'den)
- `karma` (welcome bonus 100, opsiyonel)
- `onboarding_completed = false`

Ayrıca onboarding completion endpoint'i:
- `karma += 100` (welcome bonus burada da eklenebilir, signup yerine)
- `interests`, `city`, `birth_year` set
- `onboarding_completed = true`

**Pattern memo:** `docs/test/_patterns/2026-04-26-signup-profile-insert.md` (Pattern D)
**Routing:** auth-capacitor (lead) + supabase-backend (RPC veya trigger)

---

### Verify edilemedi (yeni signup gerek)

Mevcut user'la regression yaptım — middleware otomatik onboarding'e attı. Bu yüzden:
- BUG-002 OTP verify flow (signup → /auth/verify)
- BUG-003 mission detail kategori chip
- BUG-006 KVKK native checkbox
- BUG-007 light mode 3-katman delta net görünür mü (subjective gözlem)

**Sonraki regression:** Yeni e-mail (`+t2@gmail.com` veya `+t3`), incognito tab'la baştan signup yap. Sprint Vol-2 fix sonrası.

---

### Sprint Vol-2 önerisi (3 yeni bug)

| # | Severity | Bug | Routing | Effort |
|---|----------|-----|---------|--------|
| BUG-010 | P0 | Onboarding light mode invisible | frontend-engineer (theme-blind sweep onboarding/*) | ~1 saat |
| BUG-011 + BUG-005 | P0 + P1 | Pattern D — signup profile insert eksik | auth-capacitor + supabase-backend | ~2 saat |

**Karar:** Faz 2'ye P0 bug sıfırlanmadan geçilemez. Sprint Vol-2 önce.

---

## Handoff Log (Regression)

- 2026-04-26 15:45 — test-engineer ✅ — Regression pass tamam. 3 fix verified (BUG-001, BUG-004, BUG-008), 4 deferred (yeni signup gerek), 3 yeni bug (BUG-010, BUG-011, BUG-005 regression). Pattern D yeni memo açıldı.
