import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function DonateGiveLoading() {
  return <ListPageLoading title="Bağış akışı yükleniyor" itemCount={3} itemHeight={120} />
}
