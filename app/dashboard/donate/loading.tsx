import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function DonateLoading() {
  return <ListPageLoading title="Bağış sayfası yükleniyor" itemCount={4} itemHeight={200} />
}
