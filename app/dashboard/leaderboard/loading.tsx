import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function LeaderboardLoading() {
  return <ListPageLoading title="Sıralama yükleniyor" itemCount={10} itemHeight={68} />
}
