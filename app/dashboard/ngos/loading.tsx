import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function NgosLoading() {
  return <ListPageLoading title="Kuruluşlar yükleniyor" itemCount={8} itemHeight={140} />
}
