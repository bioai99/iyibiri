import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function MyMissionsLoading() {
  return <ListPageLoading title="Görevlerim yükleniyor" itemCount={4} itemHeight={160} />
}
