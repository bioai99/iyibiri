import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function SavedLoading() {
  return <ListPageLoading title="Kaydedilenler yükleniyor" itemCount={4} itemHeight={160} />
}
