import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function ProfileDonationsLoading() {
  return <ListPageLoading title="Bağışlarım yükleniyor" itemCount={5} itemHeight={100} />
}
