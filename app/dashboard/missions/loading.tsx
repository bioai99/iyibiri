import { ListPageLoading } from '@/components/ui/state/page-loading'

export default function MissionsLoading() {
  return <ListPageLoading title="Görevler yükleniyor" itemCount={6} itemHeight={180} />
}
